// Add to src/modules/users/models/user.model.ts

export interface IUser extends Document {
    name: string;
    mobile: string;
    email?: string;
    password?: string;  // ← Add this
    role: string;
    status: string;
    avatarUrl?: string;
    lastLoginAt?: Date;
    isVerified: boolean;
    // ... other fields
}

// Add password field to schema
password: {
    type: String,
    select: false,  // Don't return password in queries
    minlength: 6
}
